import React from 'react';
import ReactDOM from 'react-dom/client';
import CarRegister from './components/register-car';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <CarRegister />
  </React.StrictMode>
);